pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        echo 'Building the project...'
        // Example: sh 'make build'
      }
    }

    stage('Deploy to SIT') {
      steps {
        echo 'Deploying to SIT...'
        // Example: sh './deploy-sit.sh'
      }
    }
  }
}
